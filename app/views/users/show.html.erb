<h1>Profile</h1>
<p>
  Welcome, <%= @user.name %>!
</p>
<% if @user.image? %>
<img src="<%= @user.image %>" />
<% end %>
<%= link_to "Edit Profile", edit_user_path(@current_user), method: :get, class: "btn" %>
